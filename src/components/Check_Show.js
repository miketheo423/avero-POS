import React, { Component } from 'react';
import { Link } from 'react-router-dom';

import Header from './Header';

class CheckShow extends Component {
  render() {
    return (
      <div>Check Show Component</div>
    );
  }
}

export default CheckShow;
